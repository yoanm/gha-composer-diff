// Package basewrapper package provides the core logic for handling the diff generation and summarization.
// It is designed to be reusable and independent of the GitHub Actions environment, allowing for easier testing
// and potential reuse in other contexts (e.g. different package manager).
//
// Should eventually be moved outside this repository to make it a standalone library and reuse it in
// other contexts (e.g. different package manager).
package ddgha
